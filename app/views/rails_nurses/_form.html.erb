<%= form_with(model: rails_nurse) do |form| %>
  <% if rails_nurse.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nurse.errors.count, "error") %> prohibited this nurse from being saved:</h2>

      <ul>
        <% nurse.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :氏名, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :ラダーレベル, style: "display: block" %>
    <%= form.number_field :ladder_level, in: 1..5 %>
  </div>

  <div>
    <%= form.label :チーム, style: "display: block" %>
    <%= form.select :team_id, @teams.map { |t| [t.name, t.id] } %>
  </div>

  <div>
    <%= form.submit "登録", class:"btn btn-secondary" %>
  </div>
<% end %>
