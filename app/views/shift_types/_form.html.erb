<%= form_with(model: shift_type) do |form| %>
  <% if shift_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shift_type.errors.count, "error") %> prohibited this shift_type from being saved:</h2>

      <ul>
        <% shift_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :kind, style: "display: block" %>
    <%= form.number_field :kind %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
